#变量=input(提示语)
#其首先会在屏幕中显示出提示语，用户输入内容，然后把用户输入的内容交给前面的变量
#坑：input()得到的结果一定是字符串
#例子
a=input('请输入第一个数字：')
print(type(a))#可以用type来查看一个变量的数据类型
b=input('请输入第二个数字：')
print(a+b)
#所以怎么把字符串转化为数字类型
#在python中，想把xxx转化成谁，就用谁套起来
#如
a=int(a)
b=int(b)
print(a+b)
